<h1>Index Page</h1>



<div class="button-container">
    <%= link_to '新しい投稿', new_user_path %>
</div>

<div>
    <div>

        <% @users.each do |post| %>
    
        <div class="m-5j p-3 border">
            <h2><%= post.name %></h2>
            <p><%= post.email %></p>
            
    
            <div class="d-flex flex-row">
                <div class="p-2"><%= link_to '編集', edit_user_path(post) %></div>
                <div class="p-2"><%= button_to '削除', user_path(post), method: :delete %></div>
                <div class="p-2"><%= link_to '詳細へ', user_path(post.id) %></div>
            </div>
        </div>
    
        <% end %>
    </div>
</ul>